[project]
name = "ai-client-api"
version = "0.1.0"
description = "Shared abstract API for AI client integrations."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/issue_tracker_client_api"]
